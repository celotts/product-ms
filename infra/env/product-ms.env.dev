# Perfil de ejecución
SPRING_PROFILES_ACTIVE=dev
PROFILE=dev

# Git Config Server
GIT_USERNAME=celotts
CONFIG_USER=configuser
CONFIG_USER_PASSWORD=a501082a-0573-4ff0-9522-d5c3d4b2e859

# PostgreSQL para product-ms
POSTGRES_DB=product
POSTGRES_USER=product
POSTGRES_PASSWORD=product123
POSTGRES_PORT=5434

# Spring Config Server URL (cambia localhost por nombre del contenedor dentro de Docker si aplica)
SPRING_CONFIG_IMPORT=optional:configserver:http://${CONFIG_USER}:${CONFIG_USER_PASSWORD}@config-service:7777

# URL JDBC para Docker (importante que use `product-db` como host si está en Compose)
SPRING_DATASOURCE_URL=jdbc:postgresql://product-db:5432/product
SPRING_DATASOURCE_USERNAME=product
SPRING_DATASOURCE_PASSWORD=product123

# Eureka
EUREKA_CLIENT_ENABLED=true
EUREKA_CLIENT_REGISTER_WITH_EUREKA=true
SPRING_EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://registry-service:8761/eureka/

# Puerto de la app
SERVER_PORT=8085

# Config extras para cloud config (opcional)
CLOUD_CONFIG_ENABLED=true
CLOUD_CONFIG_IMPORT_CHECK=false
SERVER_GIT_CLONE_ON_START=true
SERVER_GIT_TIMEOUT=10

# Management/Actuator opcional
MANAGEMENT_ENDPOINTS_ENABLED_BY_DEFAULT=false
MANAGEMENT_METRICS_ENABLED_ALL=false
MANAGEMENT_HEALTH_DEFAULT_ENABLED=false
SYSTEM_METRICS_ENABLED=false